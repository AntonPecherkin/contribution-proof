services:
  contribution-proof:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      # Host-local only; nginx terminates TLS and proxies to it. The host port is a
      # variable because this box already runs a dozen services and 3020 was taken.
      - "127.0.0.1:${HOST_PORT:-3040}:3020"
    environment:
      SUPABASE_URL: ${SUPABASE_URL}
      SUPABASE_SECRET_KEY: ${SUPABASE_SECRET_KEY}
      BRIGHTDATA_API_KEY: ${BRIGHTDATA_API_KEY}
      BRIGHTDATA_X_PROFILE_DATASET: ${BRIGHTDATA_X_PROFILE_DATASET}
      # MOCK=1 serves fixtures with no network and no database - the event-day fallback
      # if collection fails and you need the booth to keep working.
      MOCK: ${MOCK:-}
    restart: unless-stopped
