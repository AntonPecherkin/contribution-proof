/** Implemented by task C2. Signature is part of the seam — do not change it. */
export type EvidenceBand = 'good' | 'limited' | 'directional';

export function evidenceBand(_eligibleCount: number): EvidenceBand | null {
  throw new Error('not implemented — see docs/tasks/C2-normalize-evidence.md');
}
